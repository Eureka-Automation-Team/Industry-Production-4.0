@using Microsoft.AspNetCore.Identity
@using IndustryProduction.Data
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

@*<ul class="navbar-nav">
        @if (SignInManager.IsSignedIn(User))
        {
            <div class="image">
                <img src="~/dist/img/user2-160x160.jpg" class="img-circle elevation-2" alt="User Image">
            </div>
            <li class="nav-item">
                <a class="info" style="padding: 1px;" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @User.Identity.Name!</a>
            </li>
            <li class="nav-item">
                <form class="info" style="padding: 1px;" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                    <button type="submit" class="nav-link btn btn-link text-white">Logout</button>
                </form>
            </li>
        }
        else
        {
            <li class="nav-item">
                <a class="info" style="padding: 1px;" asp-area="Identity" asp-page="/Account/Register">Register</a>
            </li>
            <li>
                <a class="info" style="padding: 1px;" asp-area="Identity" asp-page="/Account/Login">Login</a>
            </li>
        }
    </ul>*@
@*<div class="row">
    <div class="info">
        <a class="info" style="padding: 1px;" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">@User.Identity.Name!</a>
    </div>
</div>
<div class="row">
    <form class="info" style="padding: 1px;" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
        <button type="submit" class="nav-link btn btn-link text-white">Logout</button>
    </form>
</div>*@

<ul class="nav sec-nav navbar-nav flex-row">
    <li class="user-item nav-item dropdown">
        <a class="user-link nav-link d-inline-flex align-items-center h-100 small-1 pl-1 pl-sm-3 pr-0" href="#" id="dropdownAdmin_02" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <img class="user-avatar rounded-circle mr-sm-3" src="/Themes/Beid/assets/img/avatar/1.jpg" alt="Avatar">
            <div class="d-none d-sm-block lh-1">
                <div class="lh-5">@User.Identity.Name</div>
                <span class="small-3"><i class="fas fa-circle text-warning small-5"></i> Online</span>
            </div>
        </a>
        <div class="dropdown-menu dropdown-menu-right shadow-1 py-3 position-absolute mt-2" aria-labelledby="dropdownAdmin">
            <a class="dropdown-item" href="#"><span class="icon-messenger-user-avatar mr-2"></span>My Profile</a>
            <a class="dropdown-item" href="#">
                @*<span class="icon-power-on-buton mr-2"></span>Logout*@
                <form class="info" style="padding: 1px;" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                    <button type="submit" class="nav-link btn btn-link text-white">Logout</button>
                </form>
            </a>
        </div>
    </li>
</ul>